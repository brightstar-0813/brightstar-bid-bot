{
  "name": "brightstar-auto-apply-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check background.js && node --check autofill-runner.js && node --check popup.js && node --check ats-score.js && node --check content/autofill.js",
    "test": "node --test tests"
  }
}
